# Anthropic API
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-6
# Timeout for Anthropic API calls in seconds (default: 60)
# ANTHROPIC_TIMEOUT=60

# Which backend the registry should use. Only "anthropic" exists right now.
MODEL_BACKEND=anthropic

PG_HOST=
PG_PORT=
PG_DBNAME=
PG_USER=
PG_PASSWORD=

# Directory for persistent data (history). Docker/cloud only — do not set for
# local development. docker_run.sh passes -e CANOPY_DATA_DIR=/data explicitly.
# Without this set, the app defaults to ~/.canopy and falls back to that path
# if the configured directory cannot be created.
# CANOPY_DATA_DIR=/data